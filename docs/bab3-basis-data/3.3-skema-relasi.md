# 3.3 Skema Relasi Database (Entity-Relationship / Relationship Window)

Diagram di bawah ini digambar dengan Mermaid (`erDiagram`), mirip tampilan "Relationship Window" di MySQL Workbench atau tab "Designer" di phpMyAdmin. Setiap kotak adalah satu tabel, garis menunjukkan relasi antar tabel lewat foreign key.

Versi visual yang lebih rapi juga dipublikasikan sebagai Artifact (link diberikan terpisah di chat) — isinya sama persis, hanya lebih enak dilihat/discreenshot untuk ditempel ke proposal.

```mermaid
erDiagram
    MASTER_RESIDENTS {
        int id PK
        varchar bca_id
        varchar full_name
        varchar room_number
        varchar class_name
        varchar gender
        varchar resident_status
        varchar phone_number
        varchar email
        datetime created_at
        datetime updated_at
    }
    ACCOUNTS {
        int id PK
        int resident_id FK
        varchar bca_id
        varchar full_name
        varchar role
        varchar password_hash
        tinyint is_active
        tinyint must_change_password
        datetime created_at
    }
    SECURITY_STAFF {
        int id PK
        varchar bca_id
        varchar full_name
        varchar gender
        varchar staff_status
        datetime created_at
        datetime updated_at
    }
    PERMITS {
        int id PK
        int resident_id FK
        varchar permit_code
        varchar qr_token
        varchar destination
        varchar permit_type
        datetime planned_departure_at
        datetime planned_return_at
        varchar entry_code
        varchar status
        datetime created_at
    }
    PERMIT_EVENTS {
        int id PK
        int permit_id FK
        int performed_by_account_id FK
        varchar event_type
        datetime occurred_at
    }
    AUDIT_LOGS {
        int id PK
        int actor_account_id
        varchar action
        varchar entity_type
        varchar entity_id
        datetime created_at
    }
    LOGIN_ATTEMPTS {
        int id PK
        varchar identifier
        varchar action
        datetime attempted_at
    }
    MANAGER_BOOTSTRAP_LINKS {
        varchar bootstrap_bca_id PK
        int account_id FK
    }
    BROADCAST_NOTIFICATIONS {
        int id PK
        varchar title
        text body
        int created_by_account_id FK
        datetime created_at
    }
    NOTIFICATION_DELIVERIES {
        int notification_id PK,FK
        int account_id PK,FK
        datetime read_at
    }

    MASTER_RESIDENTS ||--o| ACCOUNTS : "punya akun login (khusus mahasiswa)"
    MASTER_RESIDENTS ||--o{ PERMITS : "mengajukan"
    PERMITS ||--o{ PERMIT_EVENTS : "punya riwayat status"
    ACCOUNTS ||--o{ PERMIT_EVENTS : "memproses (satpam/mahasiswa)"
    ACCOUNTS ||--o| MANAGER_BOOTSTRAP_LINKS : "akun pengelola awal"
    ACCOUNTS ||--o{ BROADCAST_NOTIFICATIONS : "membuat pengumuman"
    BROADCAST_NOTIFICATIONS ||--o{ NOTIFICATION_DELIVERIES : "dikirim ke"
    ACCOUNTS ||--o{ NOTIFICATION_DELIVERIES : "menerima notifikasi"
```

## Catatan tentang tabel yang tidak terhubung langsung

- `SECURITY_STAFF` adalah data induk petugas satpam, tapi **tidak** dihubungkan dengan foreign key ke `ACCOUNTS`. Akun login satpam tetap dibuat di tabel `ACCOUNTS` (dengan `role = 'SECURITY'`), sedangkan datanya dicocokkan lewat `bca_id` yang sama di level aplikasi, bukan lewat relasi database. Pola ini sama dengan `MASTER_RESIDENTS`, hanya saja untuk mahasiswa relasinya sudah dibuat formal lewat `accounts.resident_id`.
- `AUDIT_LOGS` dan `LOGIN_ATTEMPTS` adalah tabel log/pencatatan aktivitas sistem (siapa melakukan apa, percobaan login gagal untuk mencegah brute-force). Keduanya berdiri sendiri, tidak diberi foreign key ke tabel lain, karena fungsinya hanya sebagai catatan historis, bukan bagian dari alur transaksi utama.
